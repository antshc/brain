---
name: crew-build
description: Build the project and check LSP availability. Apply during the BUILD step, before implementation.
---

# Build

**Build** the project in your workspace using the "Build the solution" instructions in `$HARNESS_REPO_PATH/README.md`. On failure, report and stop — never explore a broken build.

**Emit**: "Build: pass | fail."
