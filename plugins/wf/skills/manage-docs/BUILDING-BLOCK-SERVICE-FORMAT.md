# {{SERVICE_NAME}}

<!-- Purpose/Responsibility: 1-3 sentences on what the service is (container/process name, deployment type — e.g. feature module vs. independent service) and its core responsibility. The `{{SERVICE_NAME}}` placeholder appears only in this top heading; all section headings below are plain. -->

## API Contracts

*(optional)* {{CONTRACT_SURFACE_DESCRIPTION}}. {{CONTRACT_DOC_LINK}} <!-- agent to fill in during design -->

## Tweaks/Configuration

*(optional)* {{TWEAKS_DESCRIPTION}}. {{TWEAKS_DOC_LINK}} <!-- agent to fill in during design -->

## Persisted data

*(optional)* {{PERSISTED_DATA_DESCRIPTION}}. {{PERSISTED_DATA_DOC_LINKS}} <!-- agent to fill in during design -->

## Source code folder structure

<!-- A tree of the service's source folders with a one-line description per folder/layer. Number of layers varies per service (e.g. main application layer, platform integration layer, core layer). -->

### Layers (`{{LOCATION}}/` folder)

*(optional)* <!-- A tree of the service's source folders with a one-line description per folder/layer. Number of layers varies per service (e.g. main application layer, platform integration layer, core layer). -->

## Concepts

<!-- Bullet list, 0-N entries, each a brief description of an architectural/domain concept specific to the service, sufficient for navigation, plus a reference/link for detail (agent to fill in during design). Cross-module dependency rules, when applicable, belong here as one entry. -->

## Key features

*(optional)* <!-- 1-N bullet items, each a short named feature/capability with a one-line description. -->
