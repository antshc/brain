# Verify Fallback

- Run LSP diagnostics on changed files.
- Read the invocation directory's `README.md` and project configuration to discover build, test, and repository-specific checks.
- Run the discovered build command once for each affected module.
- Run focused tests for affected behavior.