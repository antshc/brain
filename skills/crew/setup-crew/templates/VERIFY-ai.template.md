# VERIFY — AI Authoring

<!-- Followed in order by crew-feedback Step 1 during FEEDBACK LOOPS. Hazard rules below are AI-authoring-general and ship with this template — kept even when repository practice differs; a conflict is recorded in the shared GOTCHAS.md instead of edited here. Verify steps list this repo's own checks — never invent or copy example values from another repo. When this file exists it replaces the skill's inline default for this Stack's files. -->

## Hazard rules (AI authoring)
- There is no compiler for prose — "diagnostics" means checking frontmatter (`name`/`description`) and that every skill/agent name a document invokes actually exists, not running a linter that isn't there.

## Verify steps
<!--
1.  Diagnostics: e.g. frontmatter checks, broken-link checks, invoked-skill-exists checks.
2.  Build: this repo's own step (if any) that assembles or packages skills/agents/templates.
3.  Test: the command(s), if any, that run this repo's eval/validation scripts for skills/agents.
4.  Project-specific checks: anything else this repo requires before a skill/agent/template change is considered verified.
-->
